[build]
  command = "npm run build"
  publish = "dist"

# Client-side routing (react-router BrowserRouter) needs all paths to fall back to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
